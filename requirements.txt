# Core data science and web framework dependencies
streamlit>=1.28.0
pandas>=2.0.0
plotly>=5.17.0
numpy>=1.24.0

# Data visualization and analysis (for notebooks)
matplotlib>=3.7.0
seaborn>=0.12.0

# Additional utilities that might be useful
pathlib2>=2.3.7; python_version < "3.4"

# Optional: Jupyter notebook dependencies (if you want to run notebooks)
jupyter>=1.0.0
ipykernel>=6.25.0

# Optional: Database connectivity (since sqlite3 is imported in notebooks)
# sqlite3 is built into Python, no separate package needed

# Optional: Enhanced data processing
openpyxl>=3.1.0  # For Excel file support
xlsxwriter>=3.1.0  # For Excel writing capabilities

# Development and testing (optional)
pytest>=7.4.0
black>=23.7.0
flake8>=6.0.0

# Production deployment (optional)
gunicorn>=21.2.0  # For production WSGI server